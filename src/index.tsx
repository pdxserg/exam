const control = {
	name: "Marina",
	age: 39,
	technologies: ["HTML", "", "REACT"]
}

const [science = "FOO", presence = "BOO", friendship = "DOO"] = control.technologies

if(!XXX){
	alert("HEY")
}

// Какую переменную следует указать вместо XXX, что бы была вызвана функция alert?