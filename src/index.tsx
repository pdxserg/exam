//@ts-ignore
const value = [0,[1,0]][1][0]
let quantity;

switch (value) {
	case 0:
		quantity = 24
		break;
	case 1:
		quantity = 59
		break;
	default:
		quantity = 22
		break;
}

// Какое значение получит переменная quantity?